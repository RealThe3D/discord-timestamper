# Discord Timestamper

This is a NPX script that allows the user to input data into their terminal to generate a Discord timestamp.

All source code is found in `index.ts`.

Inquirer is used to scaffold the CLI.
